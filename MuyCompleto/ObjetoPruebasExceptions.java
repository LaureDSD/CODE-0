package MuyCompleto;

public class ObjetoPruebasExceptions /* implements Comparable<ObjetoPruebasExceptions> */ {
    private String nombre;
    private int edad;
/*
    public ObjetoPruebasExceptions(String nombre, int edad) throws ExcepcionPersonalizada {
        if (nombre.length() >= 3){
            this.nombre = nombre;
        }else{
            throw new ExcepcionPersonalizada("Caracteres insuficientes");
        }
        if(edad>0) {
            this.edad = edad;
        }else{
            throw new ExcepcionPersonalizada("No admite valores negativos");
        }
    }





    public String getNombre() {
        return nombre;
    }
    public void setNombre(String nombre) throws Exception {
        if (nombre.length() >= 3){
            this.nombre = nombre;
        }else{
            throw new Exception("Caracteres insuficientes");
        }
    }





    public int getEdad() {
        return edad;
    }
    public void setEdad(int edad) throws ExcepcionPersonalizada {
        if(edad>0) {
            this.edad = edad;
        }else{
            throw new ExcepcionPersonalizada("No admite valores negativos");
        }
    }




    //public int compareTo(SubObjeto so) {
    //    return (this.valorStringSub).compareTo(so.valorStringSub);
    //}

    //public int compareTo(SubObjeto g) {
    //   if (this.valorStringSub.equals(g.getValorStringSub())) {
    //       return Integer.compare(this.numSub, g.getNumSub());
    //   }
    //   return (this.valorStringSub).compareTo(g.getValorStringSub());
    //}

    public int compareTo(ObjetoPruebasExceptions c) {
        return (this.nombre).compareTo(c.getNombre());
    }

    @Override
    public String toString() {
        return "Gato{" +
                "nombre='" + nombre + '\'' +
                ", edad=" + edad +
                '}';

    }

 */
}
