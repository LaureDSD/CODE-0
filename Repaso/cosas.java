package TEMA0;

public class cosas {

        //split(String regex): Divide una cadena en subcadenas utilizando una expresión regular como delimitador. Útil para separar una cadena en partes basadas en un patrón común.

        //trim(): Elimina los espacios en blanco al principio y al final de una cadena. Útil para limpiar cadenas de texto antes de procesarlas.

        //replace(char oldChar, char newChar): Reemplaza todas las ocurrencias de un carácter especificado en una cadena con otro carácter dado. Útil para realizar cambios simples en una cadena.

        //substring(int beginIndex, int endIndex): Devuelve una subcadena de la cadena original, comenzando en el índice beginIndex y extendiéndose hasta el índice endIndex - 1. Útil para extraer partes específicas de una cadena.

        //toUpperCase() y toLowerCase(): Convierten una cadena a mayúsculas o minúsculas, respectivamente. Útil para normalizar cadenas de texto antes de compararlas.

        //startsWith(String prefix) y endsWith(String suffix): Verifica si una cadena comienza o termina con la subcadena especificada. Útil para realizar comparaciones de prefijos o sufijos.

        //indexOf(String str) y lastIndexOf(String str): Encuentra la primera o última ocurrencia de una subcadena dentro de la cadena original, respectivamente. Útil para buscar subcadenas específicas.

        //charAt(int index): Devuelve el carácter en el índice especificado de la cadena. Útil para acceder a caracteres individuales en una cadena.

        //isEmpty(): Verifica si una cadena está vacía (es decir, tiene longitud cero). Útil para realizar comprobaciones de cadenas vacías.

        //contains(CharSequence s): Verifica si la cadena contiene la secuencia de caracteres especificada. Útil para verificar la presencia de una subcadena dentro de otra cadena.

        //length(): Devuelve la longitud (número de caracteres) de la cadena. Útil para obtener la cantidad total de caracteres en una cadena.

        //substring(int beginIndex): Devuelve una subcadena de la cadena original, comenzando en el índice beginIndex hasta el final de la cadena. Útil para obtener una parte de la cadena desde un índice específico hasta el final.

        //concat(String str): Concatena la cadena especificada al final de la cadena actual. Útil para unir dos cadenas en una sola cadena.

        //equalsIgnoreCase(String anotherString): Compara esta cadena con otra cadena, ignorando las diferencias entre mayúsculas y minúsculas. Útil para realizar comparaciones de cadenas sin importar la capitalización.

        //contains(CharSequence s): Verifica si la cadena contiene la secuencia de caracteres especificada. Útil para verificar la presencia de una subcadena dentro de otra cadena.

        //- Identificadores de variables en minúscula.
        //- Constantes en mayúsculas.
        //- Métodos en minúsculas.
        //- Clases con el primer carácter en mayúscula.

        //- nextInt(): lee enteros.
        //- nextDouble(): lee comas flotantes.
        //- nextLine(): lee una línea de entrada.
        //- next(): lee una palabra sin espacios.

        //!(x == i)
        //(x ==i ^ x==z)
        //(x==i && x==z) and
        //(x==i || x==z) or

        //boolean edad = true
        //edad=(x==i) Seria true si se cumple
        }